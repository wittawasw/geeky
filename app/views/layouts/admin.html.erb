<!DOCTYPE html>
<html>
  <head>
    <title>Admin Geeky</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "admin", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if session[:admin_id] %>
      <%= button_to "Logout", '/admin/sessions/1', method: :delete %>
    <% end %>
    <%= yield %>
  </body>
</html>
